name: Split saveDocumento economics

on:
  push:
    branches: ["multi"]
    paths:
      - ".github/workflows/split-save-documento.yml"

permissions:
  contents: write

jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout multi
        uses: actions/checkout@v6
        with:
          ref: multi
          fetch-depth: 0

      - name: Separa saveDocumento
        run: python3 .github/scripts/split-save-documento.py

      - name: Verifica diff
        run: git diff --check

      - name: Commit e push su multi
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/index.html
          if git diff --cached --quiet; then
            echo "Nessuna modifica da salvare"
            exit 0
          fi
          git commit -m "Separa dati operativi ed economics nei documenti"
          git push origin HEAD:multi
